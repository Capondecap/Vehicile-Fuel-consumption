<!-- Form to edit an existing fuel record, pre-filled with current values -->
<div class="container">
  <h2>Edit Fuel Record</h2>

  {{#if error}}
    <p class="error">{{error}}</p>
  {{/if}}

  <form method="POST" action="/records/{{record.id}}/edit">
    <input type="hidden" name="_csrf" value="{{csrfToken}}">

    <div>
      <label for="date">Date</label>
      <input type="date" id="date" name="date" value="{{record.date}}" required>
    </div>

    <div>
      <label for="vehicleType">Vehicle Type</label>
      <select id="vehicleType" name="vehicleType" required>
        <option value="">-- Select --</option>
        <option value="Car" {{#if (eq record.vehicleType "Car")}}selected{{/if}}>Car</option>
        <option value="Motorcycle" {{#if (eq record.vehicleType "Motorcycle")}}selected{{/if}}>Motorcycle</option>
      </select>
    </div>

    <div>
      <label for="liters">Liters</label>
      <input type="number" id="liters" name="liters" step="0.01" min="0.01" value="{{record.liters}}" required>
    </div>

    <div>
      <label for="distanceKm">Distance (km)</label>
      <input type="number" id="distanceKm" name="distanceKm" step="0.01" min="0.01" value="{{record.distanceKm}}" required>
    </div>

    <div>
      <label for="totalCost">Total Cost</label>
      <input type="number" id="totalCost" name="totalCost" step="0.01" min="0.01" value="{{record.totalCost}}" required>
    </div>

    <button type="submit">Update Record</button>
    <a href="/records">Cancel</a>
  </form>
</div>
